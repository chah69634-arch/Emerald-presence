# CC 施工任务总览

> 这是给 Claude Code（CC）施工用的任务集。每份 `cc-tasks/NN-*.md` 是一个**独立可交付单元**，可单独丢给 CC。
> 两个仓库：后端 `D:\ai\qq-st-bot`、前端桌宠 `D:\ai\Emerald-client`，各有自己的 `AGENTS.md` / `CLAUDE.md`。
> **跨仓文档**会用【后端】【前端】分节标注——CC 一次只在一个仓库工作，跨仓的请按节分两次会话交给对应仓库的 CC。

---

## 四个待定问题的决策（已拍板）

1. **梦境流动概括条（b）**：走**纯前端版**——用 HUD 现有字段（`scene_label` + `emotion_label` + EMA 趋势箭头）在前端拼一条概括，零后端改动、不增加每轮梦境延迟。后端逐轮生成概括的方案暂不做（如果纯前端版体感不够再升级）。

2. **软挽留（d）**：**做**，且按你的要求**改为更易触发**——不要求叶瑄有显性挽留情绪。触发条件：**梦境沉浸中（高 dream_depth/非快速 lucid）且情绪高涨（heat 或 emotional_tension 高）时，用户点 wake 退出 → 先插一段软挽留**，再放行退出。硬强退口子保留：用户坚持（二次确认/再点）一定能出。详见 `03-出梦主动开口与软挽留.md`。

3. **富文本 HTML（j）**：**语境/角色驱动的行内样式**——关键词变红、变大/变小。活泼角色话语更灵动，沉稳角色偶尔说重话时重点词变红。实现走现有 render-tag 契约扩展（新增 `<hl>`/`<big>`/`<sm>` 等行内标签），前端渲染为受控样式 span，其他通道由 `strip_render_tags` 通用剥除。详见后续 `06-富文本样式.md`。

4. **meta 监视（4）**：范围＝
   - 后台进程监视（**已有**）；
   - 键盘输入监视（看在写什么、撤回/删了哪些词）——**新做，隐私敏感**；
   - 识屏监视（后端 `/sensor/realtime` 端点**已有**，客户端 `sensor-service/` 采集器**空**，需补客户端 + 打开）；
   - 用「系统报错弹窗」式说话找存在感（如生气被无视时）——**新做**，复用已通的 `show_notify` 原语，做一种"假报错"样式。
   详见后续 `09-meta监视与危险模式.md`（设计先行）。

---

## 重要事实修正（影响排期）

- **邮件密码没泄露**：`config.yaml` 已在 `.gitignore`（第 16 行）且未被 git 跟踪。所以邮件项不是安全急件，只是「开开关 + 实测」。
- **出梦主动开口的代码已存在且已注册**（`core/scheduler/triggers/dream_exit.py` + `proposer_registry.py`）。你没见它触发更可能是**条件/门控**问题，先验证再说。
- **识屏后端已就绪**：`admin/routers/sensor.py` 的 `/sensor/realtime` 已能收屏幕文本与 keystroke 计数。

---

## 批次与顺序（含 CC 上下文预算建议）

每个 CC 会话别贪多：一份文档对应 1 个 PR / 1 次会话。下面按"先低风险高确定性、再大件"排。

| 批 | 文档 | 内容 | 仓库 | CC 上下文量 | 状态 |
|---|---|---|---|---|---|
| 1 | `01-梦境次数与邮件实测.md` | a 梦境次数计数 + h2 邮件开关实测 | 后端为主 + 前端1处 | 小（单会话轻松） | ✅ 已出 |
| 1 | `02-日记多角色.md` | c 日记按角色分类栏 | 前端为主 + 后端可选 | 小 | ✅ 已出 |
| 2 | `03-出梦主动开口与软挽留.md` | d 验证触发 + 软挽留 | 后端为主 + 前端wake | 中 | ✅ 已出 |
| 3 | `04-群聊管理.md` | i 回退/删除/改人数/群聊偏好栏 | 后端 + 前端 | 中（建议拆两会话：先后端API后前端） | ⏳ 待出 |
| 3 | `05-群聊互动.md` | f 角色互相接话 | 后端 arbiter+projection | 中 | ⏳ 待出 |
| 4 | `06-富文本样式.md` | j 行内 `<hl>/<big>/<sm>` | 后端契约 + 前端渲染 | 中 | ⏳ 待出 |
| 5 | `07-主题扩展栏与日夜切换.md` | h 独立栏 + 日夜双主题热切换 | 前端 | 中 | ⏳ 待出 |
| 6 | `08-潜意识审计与流动概括条.md` | b 审计脚本 + 概括条(前端) | 后端审计 + 前端 | 小–中 | ✅ 已出 |
| 7 | `09-meta监视与危险模式-设计稿.md` | 4 设计总纲（决策已定） | — | 设计 | ✅ 已出 |
| 7 | `09b-危险模式与动作接线修复.md` | 4 全局模式闸 + 修 action 名错配 | 后端 + 前端 | 中（**先做**） | ✅ 已出 |
| 7 | `09a-感知sidecar.md` | 4 进程/识屏/活动 sidecar + prompt 注入 | 前端仓Python + 后端 | 中 | ✅ 已出 |
| 7 | `09c-玩具文件沙盒.md` | 4 玩具文件读写（data/very_formal_project/） | 后端 | 中 | ✅ 已出 |
| 8 | `11-桌宠行为与存在感弹窗-设计稿.md` | g 设计总纲（决策已定） | — | 设计 | ✅ 已出 |
| 8 | `11a-桌宠行为环.md` | g state 驱动粒子/氛围 | 前端 | 中 | ✅ 已出 |
| 8 | `11b-鼠标交互.md` | g 害羞躲避/蹭/钉住 | 前端 | 中 | ✅ 已出 |
| 8 | `11c-存在感弹窗.md` | g 伪报错弹窗 + presence_nag 触发器 | 后端 + 前端 | 中–高 | ✅ 已出 |
| — | `09-activity就绪度审计` | e 活动就绪度审计 | 双仓只读 | 中 | ⏳ 未出（见下） |

> **关于 e（activity 就绪度审计）**：当初列在批次里但你的待办清单 e 本身就是"先搞清楚后端做到哪/架构如何"。这是一次只读审计，需要时我可以像老桌宠那样单独做一份 `audit-activity就绪度.md`（不改码）。目前未出——要的话说一声。

### CC 一次能改多少（经验法则）
- **一份本表里的"小"文档** = 1 次会话绰绰有余（改 2–5 个文件）。
- **"中"文档**：建议拆成"先后端 API/契约 → 再前端接线"两次会话，每次会话开头让 CC 重读对应 `AGENTS.md` 指定的 doc。
- **"大"文档（10）**：先让 CC 出设计 spec（只读 + 写 doc，不改码），你过一遍设计，再分子任务施工。

### 给 CC 的统一前置（每份文档开头都已写，这里再强调）
1. 先读该仓 `AGENTS.md`，按"任务→读哪个文档"表读对应 doc。
2. 后端：所有 `data/` 路径必须经 `core/sandbox.get_paths()`；新工具进 `_TOOL_REGISTRY`；改 `tag_rules.py` 后跑 `python tests/run_eval.py`。
3. 出错一律 graceful fallback + 日志/`console.warn`，不得白屏、不得抛到用户面前。
4. 完成后按文档末尾"验收"自查 + 跑相关测试。
